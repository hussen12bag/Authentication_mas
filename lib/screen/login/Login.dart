import 'package:flutter/material.dart';
import 'package:new_auth/screen/login/components/body.dart';

class Login extends StatelessWidget {
  Login({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
